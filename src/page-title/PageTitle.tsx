import Typography from "@mui/material/Typography";

export default function PageTitle() {
  return (
    <Typography variant="h4" component="header" sx={{ mt: 5 }}>
      Weather Application
    </Typography>
  );
}
